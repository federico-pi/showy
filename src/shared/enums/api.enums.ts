/**
 * Flow types for the api call
 */
export enum REQUEST_TYPES {
  SEARCH = 'search',
  GENERIC = 'generic',
}

/**
 * Show airing status
 */
export enum SHOW_STATUS {
  ENDED = 'Ended',
  RUNNING = 'Running',
}
